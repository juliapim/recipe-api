
name: Content Upload
description: File a bug report
body:
  
  - type: input
    id: title
    attributes:
      label: Title of document
      description: "Title of document"
    validations:
      required: true  
  - type: input
    id: url
    attributes:
      label: Url where the document can be found
      description: "Please provide the full url of your content to be uploaded."
     
    validations:
      required: true      
  - type: input
    id: url
    attributes:
      label: Url where the document can be found
      description: "Please provide the keywords for this document. They must be separated by semi-colon"
     
    validations:
      required: true
      
  - type: input
    id: metadata
    attributes:
      label: Metadata Link
      description: "Please provide, if available, the url to the metadata."
     
   
 
 

